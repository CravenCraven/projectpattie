---
title: "Your post title here"
description: "One sentence subtitle that also serves as the SEO description."
date: 2026-05-04
category: sysadmin
readTime: 7
---

<div class="pp-meta"><span>sysadmin</span> · May 4, 2026 · 7 min read</div>

<h1 class="pp-title">Your title here.<br/><em>optional pink subtitle bit.</em></h1>

<p class="pp-subtitle">Same line as description, displayed bigger.</p>

<div class="pp-toc">
  <div class="pp-toc-label">// <span>what we're getting into</span></div>
  <ol>
    <li><a href="#section-one">First section</a></li>
    <li><a href="#section-two">Second section</a></li>
    <li><a href="#checklist">Final checklist: confirm everything works</a></li>
  </ol>
</div>

<h2 class="pp-h2" id="section-one">First section</h2>

<p>Regular paragraph text. Inline <code>code looks like this</code>. Use color spans for emphasis: <span class="c-pink">pink</span>, <span class="c-cyan">cyan</span>, <span class="c-green">green</span>, <span class="c-orange">orange</span>.</p>

<p><span class="c-pink">A bold pink lead-in.</span> Then the rest of the sentence.</p>

<div class="pp-code"><div class="pp-code-head"><div class="pp-code-dots"><div class="pp-code-dot" style="background:#ff5f57"></div><div class="pp-code-dot" style="background:#febc2e"></div><div class="pp-code-dot" style="background:#28c840"></div></div><div class="pp-code-lang">bash</div><button class="pp-code-copy" onclick="navigator.clipboard.writeText(this.closest('.pp-code').querySelector('pre').textContent);this.textContent='copied!';setTimeout(()=>this.textContent='copy',1500)">copy</button></div><div class="pp-code-body"><pre>{`# multi-line code goes inside the {\` and \`}\nsudo dnf install something\nls -la`}</pre></div></div>

<h2 class="pp-h2" id="section-two">Second section</h2>

<p>More content here.</p>

<div class="pp-note"><strong>note:</strong> Orange-bordered callout for asides, warnings, footnotes.</div>

{/*
  ════════════════════════════════════════════════════════════
  FINAL CHECKLIST — every post ends with one. Customize the
  items so they actually verify the work this post described.
  Each item should be either a runnable command or a clear
  "did you do X?" eyeball check.
  ════════════════════════════════════════════════════════════
*/}

<h2 class="pp-h2" id="checklist">Final checklist: confirm everything works</h2>

<p>If every check below passes, this post's work is done and verified.</p>

<div class="pp-code"><div class="pp-code-head"><div class="pp-code-dots"><div class="pp-code-dot" style="background:#ff5f57"></div><div class="pp-code-dot" style="background:#febc2e"></div><div class="pp-code-dot" style="background:#28c840"></div></div><div class="pp-code-lang">bash</div><button class="pp-code-copy" onclick="navigator.clipboard.writeText(this.closest('.pp-code').querySelector('pre').textContent);this.textContent='copied!';setTimeout(()=>this.textContent='copy',1500)">copy</button></div><div class="pp-code-body"><pre>{`# 1. First check — replace with a real command\nsome --command\n#    -> expected output\n\n# 2. Second check\nanother --command\n\n# 3. Eyeball check (no command, just verify in the UI)\n#    -> open whatever, confirm something is true\n\n# 4. Add as many as the post needs`}</pre></div></div>

<p>Brief closing sentence about what's next or what was accomplished.</p>

<div class="pp-next">
  <div class="pp-next-label">next post</div>
  <div class="pp-next-title">Title of the next thing in the series or recommended follow-up</div>
</div>
